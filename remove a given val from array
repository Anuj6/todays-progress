class Solution {
public:
    int removeElement(vector<int>& nums, int val) {
         int n=nums.size();
         int j=0;
        for(int i=0; i<n; i++){
            if(nums[i]!=val){
                nums[j]=nums[i];
                j++;
            }
        }
        return j;
    }
};

think from both sides either it can be present tab apan kya karenge and when it is not presnt tab apan kya karenge
